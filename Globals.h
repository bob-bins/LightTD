#pragma once

const int MAP_WIDTH = 690; //must be multiple of COORD_BOX_SIZE
const int MAP_HEIGHT = 600; //must be multiple of COORD_BOX_SIZE
const int COORD_BOX_SIZE = 30;
const int DISP_WIDTH = MAP_WIDTH + 210;
const int DISP_HEIGHT = MAP_HEIGHT;
